
//TODO Will skip cross-page testing at this point. 
/*var Browser = require('phantomjs'),
    assert = require('chai').assert;

    var browser;

    suite('Cross-Page Tests', function(){
        setup(function(){
            browser = new Browser();
        });

        test('requesting a group discount quote from the move free page' + 
                'should populate the referrer field', function(done){
                    var referrer = 'http://localhost:3000/nutritions/movefree';
                    browser.
                })
    })*/